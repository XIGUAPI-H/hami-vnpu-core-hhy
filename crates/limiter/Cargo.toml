[package]
name = "limiter"
version.workspace = true
edition.workspace = true

[features]
default = []
# vxpu timeslice CoreLimiter (LiteSem); off by default for smaller/faster libvnpu.so
core_scheduler = []

[dependencies]
lazy_static = { workspace = true }
libc = { workspace = true }
shared_memory = "0.12"
log = "0.4"         
env_logger = "0.11" 
ctor = "0.2"