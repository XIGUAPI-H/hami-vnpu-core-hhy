# HAMi-vnpu-core 优化版

---

## 0. 先搞懂：这个项目到底在干什么？

### 0.1 一句话

一块 Ascend NPU（加速卡）很贵，往往要**多个容器/多个业务一起用**。  
HAMi-vnpu-core 的作用就是：

1. **限制显存（HBM）**：你最多能占多少内存。  
2. **限制算力**：你最多能“算多久 / 发多少次计算”。

这样不会出现“一个大模型把卡霸死，别人全卡住”。

### 0.2 两个角色（超重要）

可以把它想成**游乐场**：

| 角色 | 对应文件 | 生活类比 |
|------|----------|----------|
| **limiter（管理员）** | `crates/limiter` 里的守护进程 | 门口发代币的管理员 |
| **libvnpu.so（安检员）** | `crates/hook` 编出来的动态库 | 每次进项目前检查“有没有代币”的安检 |

流程是这样的：

```
你的 AI 程序（比如 vLLM、训练脚本）
        │
        │  启动时 LD_PRELOAD=libvnpu.so
        │  （意思：所有 NPU 调用先经过我们）
        ▼
   libvnpu.so 安检员
        │  问：还有代币吗？
        ▼
   本地共享内存（小黑板）
        │
        ▼
   limiter 管理员（不断往小黑板上写：发多少代币、何时休息）
```

### 0.3 几个必须认识的词

| 名词 | 白话解释 |
|------|----------|
| **Kernel（算子）** | NPU 上一次真正的计算任务。一次模型推理往往要发成百上千个 kernel。 |
| **Launch** | “发射”一个 kernel，让 NPU 去算。对应 `rtKernelLaunch*` 这类函数。 |
| **Token（代币）** | 限流用的“次数票”。有票才能 launch；票用完了要等管理员再发。 |
| **Sync（同步）** | 等 NPU 把前面排队的活干完。对应 `rtStreamSynchronize` / `rtDeviceSynchronize`。 |
| **Shared memory / shmem** | 管理员和安检员都能读写的一块内存，像公共小黑板。 |
| **Futex** | Linux 里“没票就睡觉，有票再叫醒”的等待机制。 |
| **Hook（拦截）** | 用 `LD_PRELOAD` 把官方 NPU 函数“截胡”，先走我们的逻辑，再调用真正的原函数。 |

### 0.4 原始版 vs 优化版，差在哪？

用一句话：

> **原始版：每发一个 kernel，都要去柜台完整买一次票。**  
> **优化版：一次买一批票，中间只记“发了几个”，最后再统一结账。**

所以优化版主要不是“限得更狠”，而是**少打扰、少排队、少假死**，推理更快更稳。

---

## 1. 功能点：怎么打开“优化版 hook”

### 1.1 原理（白话）

同一个项目里放了两套安检逻辑：

- 默认：老安检（`hook.rs`）——每个 kernel 都买票。  
- 加开关后：新安检（`hook_optimized.rs`）——支持批量记账、同步结账等。

编译时用 feature 决定编哪一套，不能两套同时开。

### 1.2 代码解释

`crates/hook/Cargo.toml`：

```toml
[features]
default = []
kylin_lite = []
# 打开 optimized 时，顺带编入 CoreLimiter（时间片限流）
optimized = ["limiter/core_scheduler"]
```

`crates/hook/src/lib.rs`：

```rust
// 两个 feature 互斥：同时开就编译失败
#[cfg(all(feature = "optimized", feature = "kylin_lite"))]
compile_error!("...");

// 开了 optimized：把 hook_optimized.rs 当成 hook 模块
#[cfg(feature = "optimized")]
#[path = "hook_optimized.rs"]
mod hook;

// 没开：用普通的 hook.rs
#[cfg(not(feature = "optimized"))]
mod hook;
```

**怎么用：**

```bash
# 原始风格
cargo build -p hook --release

# 优化风格
cargo build -p hook --release --features optimized
```

> 提醒：编进优化代码 ≠ 所有优化都自动生效。很多能力还要靠环境变量打开（后面每个功能都会说）。

---

## 2. 功能点：原始版安检——“每次都买票”

### 2.1 原理

想象游乐场规定：**每玩一次项目，必须先去柜台刷一次卡。**  
哪怕项目只要 0.1 毫秒，也得刷卡。

原始版就是这样：AI 框架每 launch 一个 kernel，hook 就调用一次 `wait_for_token`。

优点：逻辑简单，很好理解。  
缺点：kernel 很多时（尤其大模型 decode），刷卡本身比“玩一下”还贵，CPU 忙死、还容易卡住。

### 2.2 代码解释（原始版）

`crates/hook/src/hook.rs`（和 `hami-vnpu-core-main` 里几乎一样）：

```rust
pub extern "C" fn rtKernelLaunchWithFlagV2(...) -> u64 {
    // 1) 先买票（可能排队、睡觉）
    NPU_LIMITER.wait_for_token(stm);
    // 2) 再调用真正的官方函数
    return passthrough!(..., ...);
}
```

每一行在干什么：

1. `#[unsafe(no_mangle)]` + `extern "C"`：导出成 C 风格函数名，好被 `LD_PRELOAD` 截到。  
2. `wait_for_token(stm)`：去小黑板看有没有票；没有就等。  
3. `passthrough!(...)`：找到真正的原版 RTS 函数并调用（我们只是“夹一层”，不是替代 NPU 驱动）。

原始版拦截的 launch 比较少，主要有：

- `rtAicpuKernelLaunchExWithArgs`
- `rtAicpuKernelLaunchWithFlag`
- `rtKernelLaunchWithFlagV2`
- `rtKernelLaunchWithHandleV2`
- `rtModelExecute`

**没有**拦截更基础的 `rtKernelLaunch` / `rtKernelLaunchEx`，也**没有**拦截同步函数。

---

## 3. 功能点：优化版安检入口 `ComputeLimit`——先分流，再决定怎么限

### 3.1 原理

优化版不再“一刀切每次买票”，而是进门先问几句：

1. 算力限制开了吗？没开 → 直接放行。  
2. 是不是走“时间片硬分片”（CoreLimiter）？是 → 拿时间片通行证。  
3. 是不是“整次模型执行才计费”（Iteration）？是 → 单个 kernel 放行。  
4. 否则 → 仍走买票（但买票内部可能已是 Burst 快路径）。

像机场安检：普通旅客、贵宾、过境旅客走不同通道，而不是所有人都排同一条最长队。

### 3.2 代码解释

`hook_optimized.rs`：

```rust
fn compute_hook_enabled() -> bool {
    // 环境变量 VXPU_COMPUTE_LIMIT=0 → 关闭算力限制
    if !env_bool("VXPU_COMPUTE_LIMIT", true) {
        return false;
    }
    // NPU_PRIORITY >= 100 → 视为“不限算力”
    // 优先级 100 = 想用满整卡
    ...
}

fn enter(stm: u64) -> Self {
    if !compute_hook_enabled() {
        return Self::noop();              // 通道1：不管
    }
    if llm_hook_use_core_limiter() {
        return Self {
            _core: Some(NPU_LIMITER.core_guard(stm)), // 通道2：时间片
        };
    }
    let burst = token_burst_mode();
    if burst && iteration_sched_enabled() {
        return Self::noop();              // 通道3：kernel 免费，整次再算
    }
    NPU_LIMITER.wait_for_token(stm);      // 通道4：买票（可含 Burst）
    Self::noop()
}
```

每个函数调用再套一层：

```rust
pub extern "C" fn rtKernelLaunch(...) -> u64 {
    let _lim = ComputeLimit::enter(stm);  // 进安检
    passthrough!(...);                   // 真正发射 kernel
}  // _lim 离开作用域；若是 CoreGuard，这里会自动“还时间片”
```

`_lim` 这个变量看起来“没用”，其实是 **RAII**：对象活着=占着资源，析构时自动释放。CoreLimiter 就靠这个。

---

## 4. 功能点：Hook 覆盖更全——别让人从侧门溜进去

### 4.1 原理

如果只拦高级入口、不拦基础入口，框架一旦走 `rtKernelLaunch`，就等于**没安检**。  
限流会失效，别的租户会觉得“不公平 / 被抢算力”。

优化版把常见 launch 入口都拦上。

### 4.2 代码解释

优化版新增/补齐例如：

- `rtKernelLaunch`
- `rtKernelLaunchEx`
- `rtKernelLaunchFwk`
- `rtAicpuKernelLaunch`
- 以及原始已有的 V2 / AICPU / ModelExecute

每个都是同一模式：`ComputeLimit::enter` → `passthrough`。

另外还补了 `rtMemGetInfo`（不只是 `rtMemGetInfoEx`），让程序问“还有多少显存”时，看到的是**配额后的假象**，避免框架以为整卡都是自己的。

---

## 5. 功能点：Kernel Burst（批量记账）——优化版最大的提速点

### 5.1 原理（请一定看懂）

#### 生活类比：吃饭刷卡

- **原始版**：每夹一口菜，都去收银台刷一次卡。菜便宜、刷卡贵 → 人卡在收银台。  
- **优化版（Burst）**：  
  1. 进门先刷一次，拿到“这顿饭额度”；  
  2. 中间每夹一口，服务员只在小本本上记一笔（很快）；  
  3. 吃完（同步）时，按小本本一次性结账。

AI 推理里，“一口菜”= 一个 kernel，“吃完”= `rtStreamSynchronize` 这类同步。

#### 为什么更快？

大模型一次 forward 可能有 **几百～几千个短 kernel**。  
如果每个都完整买票：

- 频繁读共享内存  
- 频繁扣 token  
- token 没了还可能睡觉（futex）  
- 管理员还要频繁切换状态  

CPU 和同步开销会把 NPU 拖慢。Burst 把这些重操作从“每个 kernel”挪到“每个同步边界”。

### 5.2 原始版怎么做

每个 launch → `wait_for_token` 完整流程。没有“记小本本”，也没有 sync 结账。

### 5.3 优化版怎么做（三步）

1. **Open（开门）**：第一次完整买票成功，标记 `kernel_burst_open = true`。  
2. **Accumulate（记账）**：后面每个 kernel 只做 `notebook++`。  
3. **Settle（结账）**：同步函数返回后调用 `end_kernel_burst()`，按本本批量扣票。

### 5.4 代码解释：快路径记账

`crates/limiter/src/worker.rs` 里 `wait_for_token_impl`：

```rust
// 如果正在 Burst 窗口里……
if burst_accounting_active() {
    let handled = PER_THREAD_CTX.with(|cell| {
        if let Some(pt) = cell.get() {
            if pt.kernel_burst_open.get() {
                // 本线程记一笔
                let nb = pt.kernel_burst_extra.get() + 1;
                pt.kernel_burst_extra.set(nb);

                // 跨线程结算时，还要在全局本本上 +1
                // （因为 vLLM 经常：A 线程 launch，B 线程 sync）
                if burst_cross_thread_settle() {
                    GLOBAL_BURST_NOTEBOOK.fetch_add(1, Ordering::Relaxed);
                }
                return true; // 告诉外面：这次不用走完整买票
            }
        }
        false
    });
    if handled {
        return; // 直接返回，超快
    }
}
```

白话拆解：

| 代码 | 意思 |
|------|------|
| `PER_THREAD_CTX` | 每个线程自己的小抽屉（线程本地存储） |
| `kernel_burst_open` | 这顿饭开没开桌 |
| `kernel_burst_extra` | 本线程记了几笔 |
| `GLOBAL_BURST_NOTEBOOK` | 全进程公共记事本（跨线程用） |
| `return true` | 这次安检结束，别再走慢路径 |

第一次不在窗口里时，会走完整 `wait_for_token_loop`，成功后再：

```rust
pt.kernel_burst_extra.set(0);
GLOBAL_BURST_NOTEBOOK.store(0, ...);
GLOBAL_BURST_OPEN.store(true, ...);
pt.kernel_burst_open.set(true);  // 开门，后面走快路径
```

### 5.5 代码解释：同步时结账

`hook_optimized.rs`：

```rust
pub extern "C" fn rtStreamSynchronize(stm: u64) -> u64 {
    // 先让 NPU 真正把活干完
    let ret = passthrough!("rtStreamSynchronize", (u64), stm);
    // 再结账
    if sync_hook_enabled() && token_burst_mode() {
        NPU_LIMITER.end_kernel_burst();
    }
    ret
}
```

`end_kernel_burst` 核心思想（`worker.rs`）：

```text
读出 notebook 里记了多少个 kernel
charge = ceil(个数 / 每个token可抵几个kernel)
从 tokens_remaining 里一次性扣 charge
扣不够 → 记成 pending，下次开门前再补（可选）
关门：kernel_burst_open = false
```

相关环境变量：

| 变量 | 白话 |
|------|------|
| `NPU_KERNEL_BURST=1` / `NPU_LLM_BURST=1` | 打开 Burst |
| `NPU_LLM_MODE=1` | LLM 友好总开关（常连带一批默认） |
| `NPU_BURST_KERNELS_PER_TOKEN` | 几个 kernel 算 1 张票（越大越松、越快） |
| `NPU_BURST_CROSS_THREAD_SETTLE` | 允许别的线程帮你结账（vLLM 常用） |
| `NPU_BURST_REPAY` | 结账欠的票，下次开门前要不要先还 |
| `VXPU_SYNC_HOOK` | 要不要在 sync 上结账（默认开） |

> **注意：** 只开 Burst 环境变量、却不编 `optimized` hook，往往**没有 sync 结账**，账会对不上。推荐一起用。

---

## 6. 功能点：Iteration 调度——“整趟车计费，不按每一站”

### 6.1 原理

有些框架用 `rtModelExecute` 表示“整次模型跑一遍”。  
如果每个内部 kernel 都买票，仍然很吵。

Iteration 模式：

- 中间 kernel：**不买票**（noop）  
- 在 `rtModelExecute` 边界：**统一计费一次**

像地铁：按进站出站计一次费，不在每一站刷一次。

代价：细粒度公平变差（别人可能感觉你“一趟车里猛算”）。

### 6.2 代码解释

```rust
// enter() 里：burst + iteration → kernel 直接放行
if burst && iteration_sched_enabled() {
    return Self::noop();
}

// 只有 ModelExecute 才真正付费
pub extern "C" fn rtModelExecute(...) -> u64 {
    if burst && iteration_sched_enabled() {
        NPU_LIMITER.wait_for_iteration_boundary(stm);
    } else {
        let _lim = ComputeLimit::enter(stm);
    }
    let ret = passthrough!(...);
    ...
}
```

环境变量：`NPU_ITERATION_SCHED=1`（硬分片仿真 `NPU_HARD_SIM=1` 也会带上这类组合）。

---

## 7. 功能点：Token 池与“债务”——为什么要有 outstanding_token_debt

### 7.1 原理

管理员往小黑板写：`tokens_remaining = 100`（还剩 100 张票）。  
安检员为了快，有时会一次拿一摞（chunk），先放自己口袋，再慢慢用。

如果管理员只看黑板上的数字，可能误以为：

> “票已经是 0 了，这轮该结束去测量了。”

但其实工人口袋里还有票。于是状态机会乱切，性能抖、甚至卡住。

所以加一个字段：**outstanding_token_debt = 工人口袋里还没还回去的票。**  
管理员要看“黑板 + 口袋”才知道真实情况。

### 7.2 代码解释

`crates/limiter/src/shmem/mod.rs`：

```rust
pub struct LocalContainerShmem {
    pub tokens_remaining: AtomicU64,       // 黑板上还剩多少票
    pub outstanding_token_debt: AtomicU64, // 已发到工人手里、尚未刷回的票
    pub workers_waiting: AtomicU32,        // 有多少人在门口等下一轮
    ...
}
```

worker 侧还有 `local_token_debt`（线程本地口袋）。用完/结束时 `flush_token_debt` 把口袋还回黑板，并安全减少 outstanding（防止减成负数绕回巨大值）。

---

## 8. 功能点：测量（Measure）——“这轮算了多久”，以及为什么原始版会假死

### 8.1 原理

管理员发一轮票后，需要知道“你们这轮实际跑了多久”，用来：

- 更新平均 kernel 时间  
- 计算下一轮该发多少票、该休息多久  

测时有两种：

| 方式 | 白话 | 准不准 | 风险 |
|------|------|--------|------|
| **GPU Event 测时** | 在 NPU 时间线上插两个标记，算间隔 | 较准 | 不能在某些特殊状态下用 |
| **Wall-clock 测时** | 看墙上挂钟过了多久 | 较粗 | 安全，但可能把“等待时间”也算进去 |

#### ACL Graph Capture 是什么？

可以理解为：框架在“录像”（capture）一段要反复播放的计算图。  
录像期间，很多设备同步操作是**非法的**。  
原始版在“正在录像”时还调用 `rtDeviceSynchronize()`，可能把进程卡死很久，最后上层（如 vLLM）报 60 秒超时。

### 8.2 原始版问题代码

`hami-vnpu-core-main/.../worker.rs`：

```rust
// 发现 stream 正在 capture
if status != 0 {
    check_rts!(rtDeviceSynchronize()); // 危险：录像中不该这么干
    duration_us = now - start;
}
```

### 8.3 优化版怎么改

```rust
// 1) 先问：正在录像吗？
capturing = (status != 0);

// 2) 录像中，或开了 light measure：只用挂钟，绝不碰设备
if (llm_light_measure_enabled() || capturing) && start_time_us != 0 {
    duration_us = get_time_us() - start_time_us;
    used_wall_clock = true;
}

// 3) 不在录像：才允许 event 测时 / device sync
if !used_wall_clock && !capturing {
    // rtEventRecord → rtStreamWaitEvent → rtEventElapsedTime ...
}

// 4) 关键：非 LLM 模式下，挂钟结果不要写进统计
//    否则会把“整批毫秒”当成“单个 kernel 平均”，后面休息时间爆炸
if used_wall_clock && !llm_mode_enabled() {
    duration_us = 0;
}
```

对应收益：

- Graph 模式不易假死（正确性/稳定性）  
- LLM 可用轻量测时少打断（性能）  
- 防止错误统计拖垮调度（间接性能）

---

## 9. 功能点：Futex 等待——从“睡死”到“定时翻身看看”

### 9.1 原理

没票时，工人不能空转烧 CPU，通常会 **futex wait（睡觉）**，管理员发票后再 **wake（叫醒）**。

原始版：一睡就可能睡到天荒地老（timeout=NULL）。  
如果叫醒信号丢了、或管理员在休息窗口，工人会一直睡。上层 RPC 一等 60 秒就超时崩溃——看起来像“优化无关的偶发故障”，其实是等待策略太硬。

优化版：最多睡大约 **500 微秒** 就醒过来自己再看一眼小黑板。没变就再睡。这叫**自愈轮询**。

类比：不是“睡到有人拍醒”；而是“设个短闹钟，自己起来看信箱”。

### 9.2 代码解释

原始版：

```rust
futex::wait(&shmem.state, STATE_MEASURING); // 无限等
```

优化版常量：

```rust
const WORKER_STATE_WAIT_POLL_US: u64 = 500;
```

`futex.rs`：

```rust
pub fn wait(...) {
    // timeout 传 0/NULL → 无限等
    libc::syscall(..., FUTEX_WAIT, ..., 0, ...);
}

pub fn wait_timeout(..., timeout_us: u64) {
    // 带 timespec → 到点自己醒
    libc::syscall(..., FUTEX_WAIT, ..., &ts, ...);
}
```

另外还有短自旋 `spin_for_state_leave`：状态可能马上变时，先空转几圈再睡，减少“睡了又立刻被叫”的内核开销（多租户时有用）。

---

## 10. 功能点：公平份额与 Rest 修复——“你该跑多久、该歇多久”

### 10.1 原理

多个人抢一张卡时，用 **优先级 priority** 分蛋糕。  
例如 priority=25，大概表示希望占约 25% 算力。

管理员每轮大致做两件事：

1. **发多少 token**（能发射多少）  
2. **跑完后休息多久（rest）**，把时间让给别人  

固定份额的理想关系：

```text
休息时间 / 运行时间 ≈ (100 - priority) / priority
```

例如 priority=25：跑 1 份，大约休息 3 份。

#### 原始/旧公式的坑

如果错误地把“休息”绑到被污染的全局平均时间（比如把整批 50ms 当成单个 kernel 平均），休息会被算成**好几秒甚至几十秒**。  
看起来像“限流太狠、业务假死”，其实是统计被污染后休息爆炸。

### 10.2 优化版怎么算（白话 + 代码）

`manager.rs` 的 `calculate_fair_share`：

```rust
// 1) 找到大家里面“最慢的平均 kernel 时间”当锚点 anchor
// 2) 我的平均 my_time
// 3) 基础票数 ≈ priority * (anchor / my_time)
//    慢的人每个 kernel 更久，同样时间里票更少，这是公平的一部分
let raw_tokens = prio_for_tokens * (anchor_speed_us / my_time);

// 4) 再乘缩放、burst 放大、elastic 借用……
tokens = apply_burst_sizing(...);
if Elastic 且没人和我抢:
    tokens *= 4 或 8;   // 空闲就多借一点，提高利用率

// 5) 本轮预计运行时间
expected_run_us = tokens * my_time;

// 6) 休息：只由本轮 expected_run_us 推导，并封顶
rest = expected_run_us * (100 - p) / p;
rest = min(rest, max_rest_wait_us);  // 例如最多 2 秒
```

**小白记忆法：**

- token：这轮能发射多少  
- expected_run：这轮大概跑多久  
- rest：跑完让出卡多久  
- 封顶：防止算飞了

---

## 11. 功能点：FCSP 连续补票——少做“测一下、停一下”

### 11.1 原理

原始常见节奏：

```text
发票 → 跑 → 票没了 → 进入测量 → 休息/下一轮 → 再发票 …
```

问题：测量和状态切换本身有成本，LLM 输出一个 token（TPOT）时会抖。

FCSP（连续 token 桶）思路：

> 尽量留在 RUNNING，按时间一点点往桶里加水（补票），别动不动停下来测量。

像加油站的慢充：边开边滴油，而不是每开一公里就停车加油、称重、再出发。

### 11.2 代码解释

```rust
fn fcsp_refill_tokens(...) -> u64 {
    if !self.fcsp_refill { return 0; }

    // 距离上次补票不到间隔 → 不补
    if since < interval { return 0; }

    // 按优先级和经过时间，计算应补多少
    add = 速率 * 优先级比例 * 经过时间;

    // 不超过桶容量
    new_val = min(current + add, cap);
    tokens_remaining = new_val;
    wake_all(...); // 叫醒正在等票的人
}
```

环境变量：`NPU_FCSP_REFILL=1`，间隔如 `NPU_FCSP_REFILL_INTERVAL_US=100`。  
单租户还可把间隔减半，补得更勤，锯齿更细、抖动更小。

---

## 12. 功能点：Elastic（弹性）——没人抢就多借点

### 12.1 原理

固定份额保证公平，但卡空着也浪费。

Elastic：

- **有人抢**：按份额来  
- **没人抢**：临时把 token 放大（借别人的空闲）  

全局小黑板上有：

- `wants_compute`：有人在门口等着要用  
- `workers_active`：有人正在算  

用来判断“现在有没有竞争者”。

### 12.2 代码解释（见上一节 `calculate_fair_share`）

```rust
if Elastic && !competitors_want_compute() {
    if 只有我一个租户 { tokens *= 更大倍数 }
    else { tokens *= 借用倍数 }
}
```

---

## 13. 功能点：CoreLimiter（时间片）——另一条限流通道

### 13.1 原理

前面说的 token 方案，是“按发射次数/批次”限流。  
CoreLimiter 更像 CPU 的时间片：

> 你拿到片就能算；片用完要 ack 交还，下次再排队拿片。

适合想要“硬分片感觉”的场景。  
`NPU_COMPUTE_SCHED=auto` 时：没人抢用时间片；有人抢退回 token 管理员方案。

### 13.2 代码解释（概念层）

`hook_optimized.rs`：

```rust
if llm_hook_use_core_limiter() {
    return Self {
        _core: Some(NPU_LIMITER.core_guard(stm)),
    };
}
```

- 创建 `CoreGuard`：内部 `acquire` 拿到时间片  
- 函数结束 Drop：`ack` 还回去  

实现主要在 `crates/limiter/src/core_scheduler.rs`（仅 `optimized` / `core_scheduler` feature 编入）。

---

## 14. 功能点：内存限额（两边都有，优化版更稳）

### 14.1 原理

算力是“能算多久”，显存是“能占多大”。  
`rtMalloc` 前先问：再申请这么多会不会超配额？超了直接返回失败。

这样框架看到的“空闲显存”也是配额内的，不会去抢别人的 HBM。

### 14.2 代码解释（两边类似）

```rust
pub extern "C" fn rtMalloc(...) -> u64 {
    if NPU_LIMITER.is_hbm_limited() {
        if NPU_LIMITER.check_memory_quota(size) == 0 {
            // 配额够：真分配，并记账
            let ret = passthrough!(...);
            NPU_LIMITER.post_alloc_hbm(...);
            return ret;
        }
        return RT_ERROR_MEMORY_ALLOCATION; // 配额不够
    }
    passthrough!(...) // 没开内存限制就原样转发
}
```

优化版额外有 DCMI 采样等，用来更准确估计“自己进程真正用了多少显存”（多租户同卡时更重要）。对小白来说先记住：**内存限额是硬闸门，和 Burst 无关。**

---

## 15. 把整条链路串起来（建议背这个）

### 15.1 原始版一次推理

```text
kernel1 → 买票 → 发射
kernel2 → 买票 → 发射
...
kernelN → 买票 → 发射
票没了 → 测量（可能危险同步）→ 休息 → 下一轮
```

### 15.2 优化版 + Burst + FCSP

```text
第一次 → 完整买票 → 打开 Burst 门
kernel2..N → 小本本 +1 → 发射（很快）
同步 → end_kernel_burst 一次性扣票
RUNNING 期间 → FCSP 按时间滴票进来
尽量少进 MEASURING；测时时若在录像则只用挂钟
```

### 15.3 你该记住的“为什么更快”

1. **少买票次数**（Burst）  
2. **少状态切换**（FCSP）  
3. **少假死/少错误休息**（capture-safe、rest 封顶、有界 futex）  
4. **可选更粗计费**（Iteration）或 **时间片通道**（CoreLimiter）

---

## 16. 小白操作手册：怎么跑起来

### 16.1 编译优化版

```bash
cd /path/to/hami-vnpu-core
cargo build -p hook --release --features optimized
# 产物一般在 target/release/libvnpu.so ，limiter 同目录
```

### 16.2 推荐环境变量（LLM 软分片）

```bash
export NPU_GLOBAL_SHM_PATH="/hami-shared-region/global_registry"
export NPU_MEM_QUOTA=10240          # 显存上限 MB，按需
export NPU_PRIORITY=25              # 约 25% 算力份额
export NPU_LLM_MODE=1
export NPU_LLM_BURST=1
export NPU_FCSP_REFILL=1
```

### 16.3 启动顺序

```bash
# 1) 先起管理员
./target/release/limiter > limiter.log 2>&1 &

# 2) 再起业务，强制预加载安检库
LD_PRELOAD=./target/release/libvnpu.so python3 your_app.py
```

### 16.4 想看 Burst 有没有生效

```bash
export NPU_BURST_TRACE=1
# 然后看 /tmp/npu_burst_trace.log
# 出现 fast_inc / open_burst / end_burst 就说明走了批量记账
```

---

## 17. 功能点速查表（复习用）

| 功能 | 一句话原理 | 关键代码 | 主要开关 |
|------|------------|----------|----------|
| 优化版 hook | 换一套安检逻辑 | `lib.rs` + `hook_optimized.rs` | `--features optimized` |
| 每次买票（原始） | 每 kernel 完整限流 | `hook.rs` | 默认 |
| ComputeLimit 分流 | 按模式走不同通道 | `ComputeLimit::enter` | `VXPU_COMPUTE_LIMIT` 等 |
| Burst | 开门买票、中间记账、同步结账 | `wait_for_token_impl` / `end_kernel_burst` | `NPU_KERNEL_BURST` / `NPU_LLM_BURST` |
| Sync settle | 同步后结账 | `rtStreamSynchronize` 等 | `VXPU_SYNC_HOOK` |
| Iteration | 整次 ModelExecute 计费 | `rtModelExecute` | `NPU_ITERATION_SCHED` |
| Token 债务 | 口袋里的票也要算 | `outstanding_token_debt` | （内部） |
| Capture-safe 测时 | 录像中禁止设备同步 | `measure_and_report_batch` | （自动） |
| Light measure | 挂钟代替 event 链 | 同上 | `NPU_LLM_LIGHT_MEASURE` |
| 有界 futex | 短闹钟自检，防睡死 | `wait_timeout` | （内部 500µs） |
| Rest 修复 | 休息按本轮预算且封顶 | `calculate_fair_share` | `NPU_MAX_REST_WAIT_US` |
| FCSP | RUNNING 里连续补票 | `fcsp_refill_tokens` | `NPU_FCSP_REFILL` |
| Elastic | 没人抢就多借 | `calculate_fair_share` | `NPU_SCHED_POLICY` |
| CoreLimiter | 时间片通行证 | `CoreGuard` / `core_scheduler.rs` | `NPU_COMPUTE_SCHED` |
| 显存配额 | malloc 前检查额度 | `rtMalloc` + `check_memory_quota` | `NPU_MEM_QUOTA` |

---

## 18. 最后的提醒（别踩坑）

1. **`--features optimized` 和一堆环境变量要配套**，只编不配，Burst 可能不完整。  
2. **Burst 会短时间“先用后付”**，不是每一瞬间都精确卡死配额。  
3. **memcpy（拷贝）路径基本不限算力**，限的是计算 launch。  
4. **同卡多租户**时，务必设好 `NPU_PRIORITY` 和全局 shmem 路径。  
5. 看不懂日志时，先开 `NPU_BURST_TRACE=1`，确认有没有 `fast_inc`。

---

